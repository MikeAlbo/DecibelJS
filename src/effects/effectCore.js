/*

* let now = new audioObject(audioBuffer, {
	volume: 1,
	reverb : {
		gain: 1,
		waveshapper: .5
	},
	lowpass: 1
});


let updateVolume = (src, level) => {
	src.volume = level;
};

// onclick
window.document.getElementById("volumeUp").addEventListener("click", ()=>{
	return updateVolume(now, now.volume++);
});

window.document.getElementById("volumeDown").addEventListener("click", ()=>{
	return updateVolume(now, now.volume--);
});

window.document.getElementById("masterVolume").addEventListener("click", ()=>{
	return masterGain.updateLevel(masterGain.getLevel++);
});
*/
